[build]
    command = "npm run generate"
    publish = "dist"

[build.processing.html]
    # Off: the trailing slash it adds (/about -> /about/) no longer matches the
    # prerendered route, and Nuxt drops the query and hash when it corrects that
    pretty_urls = false
